#!/usr/bin/env python3
import sys

# Calculate the number of parameters (excluding the script name itself)
num_params = len(sys.argv) - 1

# Print the result in the required format
print(f"Number of parameters: {num_params}.")
